language: node_js
node_js:
 - 12.22.7
cache : yarn

install:
  -yarn test
  -yarn run build

  deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GitHub_token
    on:
      branch: main


